LOW — hygiene and drift
~~README is substantially stale.~~ RESOLVED 2026-07-27 — root README §1/§5/§7/§8, pfpalette/README.md, pfanchor/README.md and OPEN-ISSUES.md all updated after the palette shakedown. OPEN-ISSUES.md is still titled "V4"; harmless, rename when convenient.
~~.odcl control names are unverifiable statically.~~ MITIGATED 2026-07-27 — `C:PFPDIAG` (pfpalette.lsp §6) resolves all 29 control names at runtime and lists any that are missing, so the binary is no longer opaque to review. Two caveats that keep this from being fully closed: (a) it must be run with the palette OPEN, because `dcl-Form-Close` destroys the child controls and every symbol reverts to nil — closed, it reports 0 of 29; (b) it proves a name RESOLVES, not that it is UNIQUE, so a duplicate `(Name)` still leaves both symbols resolving with one unaddressable. Reading the Studio browser tree is the only check for that (PALETTE-TESTING 1.5, unrun). The suspicion that lblProject/lblCounts were the failures was wrong — they resolve; their blankness was colour plus a stale project cache.
*pf-preset-target* (README §6a) was never implemented. No occurrence anywhere.
The extracted engines have no consumer. pflabel:run / pfi:run / pfxl:run are called only by their own C:PF*. There is still no C:PF*RUN and no Tab 2, so the refactor remains inert — but as of 2026-07-27 the DEFERRED FIRE IS NO LONGER MISSING: pfp:defer / pfp:cmd-idle-p are proven in CAD (PALETTE-TESTING §4) and shipped in pfpalette.lsp §2. The channel the engines were extracted FOR now exists and is tested; what is missing is the thin C:PF*RUN commands and the ticket that feeds them (root README §6b, Phase 2). Both gates that blocked that work are open.
pf:echo-off/pf:echo-on are not re-entrant (pftools-lib.lsp:36-44). Nested calls lose the user's original CMDECHO and default it to 1. No nesting path exists today (only c: prologues call it), so this is latent — but the deferred-fire work in Tab 2 is exactly the thing that would create one. RAISED IN PRIORITY 2026-07-27: the deferred fire is now PROVEN and Phase 2 is unblocked, so the path that creates the nesting is the next thing being built. Fix this before C:PFPVERB and the C:PF*RUN commands land, not after. Note pfp:ensure and C:PFPRELOAD also save/restore CMDECHO around (command "_OPENDCL") by hand — same pattern, same latent hazard.
Session directory memory is clobbered. pfsetup.lsp:92, :111, :129 overwrite *pfset-dir-cl/pro/tin* with the company folder on every pick, so the "last-browsed directory" feature in pfsettings §3 never takes effect.
The XING pass ledger only appends (pfxlabel.lsp:367-372); dead and duplicate handles accumulate across relabels.
pf:fmt-station (pftools-lib.lsp:605-607) produces 0+-50.00 for negative stations (documented caveat) and 0+100.00 for 99.999 (rounding at the boundary).
Dead API surface: pf:tin-load / pf:tin-unload / pf:tin-z are never called — the existing/DESIGN_ .tin bindings are collected, checksummed and stored but never consumed by anything. Also unused: pf:bbox, pf:get-verts, pf:pro-range, pf:y->elev, pf:text-pos, pf:remove-nth, pf:align-layer, pf:filter-layer, pfa:status-get, pfa:twin-cksum, and four pfa:xr-* accessors.

